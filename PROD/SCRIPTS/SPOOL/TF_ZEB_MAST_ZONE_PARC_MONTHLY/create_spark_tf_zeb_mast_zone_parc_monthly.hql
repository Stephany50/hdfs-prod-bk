CREATE TABLE MON.SPARK_TF_ZEB_MAST_ZONE_PARC_MONTHLY
(
    CATEGORY VARCHAR(50),
    USER_STATUS VARCHAR(200),
    SITE_NAME VARCHAR(50),
    TOWNNAME VARCHAR(50),
    ADMIN_REGION VARCHAR(50),
    COMMERC_REGION VARCHAR(50),
    TOTAL_MSISDN BIGINT,
    INSERT_DATE TIMESTAMP
)
PARTITIONED BY (BASE_MONTH VARCHAR(7))
STORED AS PARQUET
TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY')