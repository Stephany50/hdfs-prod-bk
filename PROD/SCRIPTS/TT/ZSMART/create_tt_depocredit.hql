CREATE EXTERNAL TABLE CDR.TT_DEPOCREDI
(

Customer_id VARCHAR(200),
Subs_id VARCHAR(200),
ACCNBR VARCHAR(200),
Adjust_balance_amount VARCHAR(200),
Adjust_balance_date VARCHAR(200),
ORIGINAL_FILE_NAME VARCHAR(200),
ORIGINAL_FILE_SIZE VARCHAR(200),
ORIGINAL_FILE_LINE_COUNT VARCHAR(200)
)
COMMENT 'DEPOCREDI external tables-TT'
ROW FORMAT DELIMITED FIELDS TERMINATED BY '|'
LOCATION '/PROD/TT/ZMART/DEPOCREDI_'
TBLPROPERTIES ('serialization.null.format'='');