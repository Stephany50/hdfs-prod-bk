CREATE EXTERNAL TABLE IF NOT EXISTS CTI.TT_SVI_NAVIGATION(

ID_APPEL VARCHAR(50),

ELEMENT VARCHAR(255),

DATE_ELEMENT TIMESTAMP,

TYPE_ELEMENT VARCHAR(50),

COMPLEMENT VARCHAR(50),

DATE_DEBUT_OMS TIMESTAMP,

ORIGINAL_FILE_NAME VARCHAR(50),

ORIGINAL_FILE_SIZE  INT,

ORIGINAL_FILE_LINE_COUNT  INT
)
COMMENT 'CTI SVI_NAVIGATION external tables-TT'
ROW FORMAT DELIMITED FIELDS TERMINATED BY ';'
LOCATION '/PROD/TT/CTI/SVI_NAVIGATION'
TBLPROPERTIES ('serialization.null.format'='');