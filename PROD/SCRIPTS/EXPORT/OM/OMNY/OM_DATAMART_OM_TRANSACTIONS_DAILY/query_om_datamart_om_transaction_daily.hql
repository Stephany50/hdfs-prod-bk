SELECT 
    MSISDN,
    DATE_FORMAT(PERIOD, 'dd/MM/yyyy') PERIOD,
    MSISDN_BENEFICIAIRE,
    SERVICE,
    SERVICE_DETAILS,
    MONTANT_TRANSACTION,
    FRAIS_TRANSACTION
FROM MON.SPARK_FT_DATAMART_OM_TRANS
WHERE PERIOD = '###SLICE_VALUE###'