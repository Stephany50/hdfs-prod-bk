CREATE TABLE IF NOT EXISTS MON.GLOBAL_ACTIVITY_DAILY_STATUS(
    SOURCE_DATA STRING,
    TAXED_AMOUNT BIGINT,
    INSERT_DATE TIMESTAMP
)
PARTITIONED BY (TRANSACTION_DATE DATE)
STORED AS ORC
TBLPROPERTIES ('transactional'='true',"orc.compress"="ZLIB","orc.stripe.size"="67108864")
