ALTER TABLE MON.SPARK_FT_CRM_OMNY_TRANSACTIONS_USER

DROP PARTITION TRANSFER_DATE IN (SELECT MIN(TRANSFER_DATE) FROM MON.SPARK_FT_CRM_OMNY_TRANSACTIONS_USER  WHERE TRANSFER_DATE<= '###SLICE_VALUE###' -180 )
