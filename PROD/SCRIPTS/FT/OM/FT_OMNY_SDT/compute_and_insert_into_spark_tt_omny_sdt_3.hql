INSERT INTO TMP.TT_OMNY_SDT_3
SELECT MSISDN
FROM CDR.SPARK_IT_OM_SUBSCRIBERS
WHERE (TO_DATE(REGISTERED_ON) = "###SLICE_VALUE###") AND (USER_TYPE NOT IN ('SUBSCRIBER')) AND (ACCOUNT_STATUS NOT IN ('N'))
GROUP BY MSISDN