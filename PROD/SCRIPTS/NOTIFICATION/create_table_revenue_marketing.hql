
CREATE TABLE IF NOT EXISTS MON.REVENUE_MARKETING(
    MSISDN STRING,
    SMS STRING,
    INSERT_DATE TIMESTAMP
)
PARTITIONED BY (TRANSACTION_DATE DATE)
STORED AS ORC
TBLPROPERTIES('TRANSACTIONAL'='TRUE')
;