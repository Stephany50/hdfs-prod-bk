flux.yarn.queue = "compute"
flux.log-level = "ERROR"

flux.name = "SQ_EXPORT_LMA_AUTRE_CA_PREPAID_VAS"

flux.type = "EXPORT"

flux.has-date-processing = true

flux.slice-value-type = "DAILY"
flux.slice-begin-value = -3
flux.slice-end-value = -1
flux.slice-step-value = 1
flux.slice-begin-value-offset = 0
flux.slice-end-value-offset = 0
flux.slice-date-format = "yyyy-MM-dd"

flux.rdms.has-pre-queries = true

flux.hive.has-pre-queries = true

flux.rdms.pre-exec-queries += """
SELECT (CASE WHEN NBR = 0 THEN 'OK' ELSE 'NOK' END) RESULT FROM
(SELECT COUNT(*) NBR FROM MON.LMA_CA_PRPD_VAS WHERE JOUR BETWEEN TO_DATE('###SLICE_VALUE### 000000', 'YYYY-MM-DD HH24MISS') AND TO_DATE('###SLICE_VALUE### 235959', 'YYYY-MM-DD HH24MISS'))
"""
flux.hive.pre-exec-queries += """
SELECT IF(A.nbr_zebra_trans > 0, 'OK', 'NOK') FROM
(SELECT COUNT(*) nbr_zebra_trans FROM CDR.SPARK_IT_ZEBRA_TRANSAC WHERE file_date='###SLICE_VALUE###') A
"""

flux.sqoop.jdbc-connect-uri = "jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS_LIST=(ADDRESS=(PROTOCOL = TCP)(HOST = 172.21.75.133)(PORT = 20303)))(CONNECT_DATA=(SERVICE_NAME = OCMDWH)))"
flux.sqoop.jdbc-username = "mon"
flux.sqoop.jdbc-password = "Mon123ocm#"
flux.sqoop.jdbc-driver = "oracle.jdbc.driver.OracleDriver"

flux.sqoop.query = "SELECT TRANSFER_DATE as jour, 'DATA via VAS' as type_flux, t2.osp_contract_type as profil, t2.commercial_offer as offre, SUM(TRANSFER_AMOUNT)/100 AS AMOUNT, COUNT(TRANSFER_ID) as NOMBRE FROM CDR.SPARK_IT_ZEBRA_TRANSAC t1 LEFT OUTER JOIN MON.SPARK_FT_CONTRACT_SNAPSHOT t2 ON t1.receiver_msisdn=t2.ACCESS_KEY and t2.event_date=t1.TRANSFER_DATE WHERE TRANSFER_DATE = '###SLICE_VALUE###' AND t2.event_date = '###SLICE_VALUE###' AND TRANSFER_STATUS='200' AND TRANSACTION_TYPE ='PVAS' GROUP BY TRANSFER_DATE, 'DATA via VAS', offre, profil)"

flux.sqoop.export-rdms.staging-table = "MON.SQ_LMA_AUTRE_CA_PREPAID_VAS"
flux.sqoop.export-rdms.dest-table = "MON.LMA_CA_PRPD_VAS"
flux.sqoop.export-hive.staging-table = "LMA_AUTRE_CA_PREPAID_VAS"
flux.sqoop.export-hive.staging-table-database = "TMP"

flux.sqoop.extra-params +="-Dmapreduce.map.java.opts='-Duser.timezone=UTC'"
flux.sqoop.extra-params +="-jt local"