flux.yarn.queue = "compute"
flux.log-level = "ERROR"

flux.input-type = "HIVE"
flux.output-type = "HIVE"
flux.spark.setup-conf += {"key": "hive.exec.dynamic.partition.mode","value": "nonstrict"}
flux.spark.setup-conf += {"key": "spark.sql.crossJoin.enabled", "value": "true"}
flux.spark.setup-conf += {"key": "spark.sql.session.timeZone", "value": "UTC"}
flux.spark.setup-var +=  {"key": "date_offset","value": "5"}
flux.spark.setup-var +=  {"key": "table_type","value": "FTA"}
flux.spark.setup-var +=  {"key": "table_name","value": "FT_A_APPELS_CTI"}
flux.spark.setup-var +=  {"key": "seuil_nb_ligne","value": "8000"}

flux.spark.setup-var +=  {"key": "insert_date_column","value": "INSERT_DATE"}

flux.spark.setup-var +=  {"key": "database_table_name","value": "FT_A_APPELS_CTI"}

flux.spark.setup-var +=  {"key": "table_partition","value": "EVENT_DATE"}

flux.name = "LOAD_SPARK_FT_A_APPELS_CTI"

flux.has-date-processing = true

flux.slice-value-type = "DAILY"

flux.slice-begin-value = -7

flux.slice-end-value = -1
flux.slice-step-value = 1
flux.slice-begin-value-offset = 0
flux.slice-end-value-offset = 0
flux.slice-has-state-query=true
flux.slice-state-query="""
select
    if(count(*)=7,'OK','NOK')
from dim.dt_dates
where datecode between date_sub(current_date, 7) and date_sub(current_date, 1)
    and datecode in (select distinct event_date from AGG.FT_A_APPELS_CTI where event_date between date_sub(current_date, 7) and date_sub(current_date, 1))
"""
flux.slice-has-filter-query = true
flux.slice-filter-query = """
select
    date_format(datecode,'yyyy-MM-dd')
from dim.dt_dates
where datecode between date_sub(current_date, 7) and date_sub(current_date, 1)
    and datecode in (select distinct event_date from AGG.FT_A_APPELS_CTI where event_date between date_sub(current_date, 7) and date_sub(current_date, 1))
"""
flux.slice-date-format = "yyyy-MM-dd"

flux.has-pre-queries = true
flux.has-exec-queries = true
flux.has-post-queries = false

flux.exec-query.execution.mode = "JDBC"

flux.inline.exec-queries += "set  hive.exec.dynamic.partition=true"
flux.inline.exec-queries += "set hive.exec.dynamic.partition.mode=nonstrict"
flux.inline.exec-queries += "SET hive.enforce.bucketing=false"
flux.inline.exec-queries += "SET hive.enforce.sorting=false"


flux.pre-exec-queries += "/PROD/SCRIPTS/FT/CTI/FT_A_APPELS_CTI/prequery_ft_a_appels_cti.hql"

flux.inline.exec-queries += "set hive.vectorized.execution.enabled=true"
flux.inline.exec-queries += "set hive.vectorized.execution.reduce.enabled=true"
flux.inline.exec-queries += "set hive.exec.parallel=true"
//flux.inline.exec-queries += "SET hive.exec.reducers.max=8"
flux.exec-queries += "/PROD/SCRIPTS/FT/CTI/FT_A_APPELS_CTI/compute_and_insert_into_ft_a_appels_cti.hql"

