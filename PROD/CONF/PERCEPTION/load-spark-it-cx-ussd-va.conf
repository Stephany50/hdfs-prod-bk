flux.yarn.queue = "collect"
flux.log-level = "ERROR"

flux.name = "LOAD_IT_CX_USSD_VA"

flux.has-date-processing = false

flux.hdfs.move-to-tt = true
flux.hdfs.src-temp-tt-dir-regex = "/PROD/RAW/CX_USSD_VA/USSD_VA_*"
flux.hdfs.dest-temp-tt-dir = "/PROD/TT/CX_USSD_VA"

flux.hdfs.move-to-arch = true
flux.hdfs.src-arch-dir-regex = "/PROD/TT/CX_USSD_VA/*"
flux.hdfs.dest.arch-dir = "/PROD/ARCH/CX_USSD_VA"
flux.hdfs.dest.has-sub-dir = true
flux.hdfs.dest.has-sub-dir-extractor-regex = "(\\d{8})"

flux.has-pre-queries = false
flux.has-exec-queries = true
flux.has-post-queries = true

flux.exec-query.execution.mode = "JDBC"

flux.exec-queries += "/PROD/SCRIPTS/IT/PERCEPTION_CLIENT/insert_into_spark_it_cx_ussd_va.hql"

flux.hive.extra-conf += "--hiveconf hive.exec.dynamic.partition=true"
flux.hive.extra-conf += "--hiveconf hive.exec.dynamic.partition.mode=nonstrict"
flux.hive.extra-conf += "--hiveconf hive.enforce.bucketing=true"
flux.hive.extra-conf += "--hivevar date_offset=3"
